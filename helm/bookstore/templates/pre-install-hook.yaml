#apiVersion: batch/v1
#kind: Job
#metadata:
#  name: pre-install-hook
#  labels:
#    helm.sh/hook: pre-install
#    helm.sh/hook-delete-policy: hook-succeeded
#spec:
#  template:
#    spec:
#      containers:
#        - name: pre-install
#          image: bash:latest
#          command: ["/bin/bash", "-c", "/scripts/pre-install.sh"]
#      restartPolicy: Never